[project]
name = "biokit"
version = "0.1.0"
description = "CLI toolkit for biological sequence analysis"
requires-python = ">=3.10"
dependencies = [
    "typer",
    "rich",
]

[project.scripts]
biokit = "biokit.cli:app"